# Database Setup Guide

## Issue: Missing `due_date` Column

If you're encountering the error:
```
"Could not find the 'due_date' column of 'tasks' in the schema cache"
```

This means the database schema hasn't been updated with the latest columns.

## Solution

### 1. Run Database Migrations

The following migration files need to be applied to your Supabase database:

1. **Initial tasks table**: `supabase/migrations/20260314142949_ddf21aae-3eee-41e3-86be-edef825ca761.sql`
2. **Priority and due date columns**: `supabase/migrations/20260314150000_add_priority_and_due_date.sql`
3. **Schema fix**: `supabase/migrations/20260314160000_fix_due_date_column.sql`

### 2. Apply Migrations via Supabase Dashboard

1. Go to your Supabase project dashboard
2. Navigate to **SQL Editor**
3. Run the following SQL commands:

```sql
-- Add priority column (if not exists)
DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1 
        FROM information_schema.columns 
        WHERE table_name = 'tasks' 
        AND column_name = 'priority'
        AND table_schema = 'public'
    ) THEN
        ALTER TABLE public.tasks 
        ADD COLUMN priority TEXT CHECK (priority IN ('low', 'medium', 'high')) DEFAULT 'medium';
        
        CREATE INDEX IF NOT EXISTS idx_tasks_priority ON public.tasks(priority);
    END IF;
END $$;

-- Add due_date column (if not exists)
DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1 
        FROM information_schema.columns 
        WHERE table_name = 'tasks' 
        AND column_name = 'due_date'
        AND table_schema = 'public'
    ) THEN
        ALTER TABLE public.tasks 
        ADD COLUMN due_date TIMESTAMP WITH TIME ZONE;
        
        CREATE INDEX IF NOT EXISTS idx_tasks_due_date ON public.tasks(due_date);
    END IF;
END $$;
```

### 3. Verify Schema

Run this query to verify the schema:

```sql
SELECT column_name, data_type, is_nullable, column_default 
FROM information_schema.columns 
WHERE table_name = 'tasks' 
AND table_schema = 'public'
ORDER BY ordinal_position;
```

**Expected output should include:**
- `id` (uuid)
- `user_id` (uuid)
- `title` (text)
- `description` (text)
- `status` (text)
- `priority` (text)
- `due_date` (timestamp with time zone)
- `created_at` (timestamp with time zone)
- `updated_at` (timestamp with time zone)

### 4. Restart Application

After applying the migrations, restart your development server:

```bash
npm run dev
```

## Current Database Schema

```sql
CREATE TABLE public.tasks (
  id UUID NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  title TEXT NOT NULL,
  description TEXT,
  status TEXT NOT NULL CHECK (status IN ('pending', 'in-progress', 'completed')) DEFAULT 'pending',
  priority TEXT CHECK (priority IN ('low', 'medium', 'high')) DEFAULT 'medium',
  due_date TIMESTAMP WITH TIME ZONE,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now(),
  updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now()
);

-- Indexes
CREATE INDEX idx_tasks_user_id ON public.tasks(user_id);
CREATE INDEX idx_tasks_status ON public.tasks(status);
CREATE INDEX idx_tasks_priority ON public.tasks(priority);
CREATE INDEX idx_tasks_due_date ON public.tasks(due_date);

-- RLS Policies
ALTER TABLE public.tasks ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view their own tasks" ON public.tasks
  FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "Users can create their own tasks" ON public.tasks
  FOR INSERT WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update their own tasks" ON public.tasks
  FOR UPDATE USING (auth.uid() = user_id);

CREATE POLICY "Users can delete their own tasks" ON public.tasks
  FOR DELETE USING (auth.uid() = user_id);
```

## Error Handling

The application now includes improved error handling that will:

1. Detect schema errors and provide clear messages
2. Handle null values properly for optional fields
3. Show user-friendly error messages via toast notifications

## TypeScript Types

The TypeScript types have been updated to match the database schema:

```typescript
export type Task = Tables<'tasks'>;
export type TaskStatus = 'pending' | 'in-progress' | 'completed';
export type TaskPriority = 'low' | 'medium' | 'high';

export type TaskInsert = {
  title: string;
  description?: string;
  status?: TaskStatus;
  priority?: TaskPriority;
  due_date?: string;
};
```

## Troubleshooting

If you still encounter issues:

1. **Clear browser cache** and restart the development server
2. **Check Supabase logs** for any migration errors
3. **Verify environment variables** are correctly set
4. **Ensure TypeScript types** are regenerated by restarting the TypeScript server

## Support

If the issue persists, check the browser console for detailed error messages and ensure all migrations have been successfully applied to your Supabase database.
